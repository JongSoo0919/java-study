package class_data;

public class Student {
    String name;
    int age;
    int grade;

    @Override
    public String toString() {
        return "name : " + this.name + " age : " + this.age + " grade : " + grade;
    }
}
